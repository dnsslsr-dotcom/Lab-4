expr = input("Введіть вираз: ")

balance = 0
is_balanced = True

for ch in expr:
    if ch == '(':
        balance += 1
    elif ch == ')':
        balance -= 1
        if balance < 0:
            is_balanced = False
            break

if balance != 0:
    is_balanced = False

print(is_balanced)
